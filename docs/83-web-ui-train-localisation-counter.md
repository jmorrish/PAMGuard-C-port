# Web UI train-localisation counter

The browser dashboard now includes a `Train delays` counter that reflects `clickTrainLocalisations.length` in engine responses.

This sits beside per-click delay sets, click trains, and derived train bearings so operator smoke tests can see whether multi-click train localisation summaries are being produced.
