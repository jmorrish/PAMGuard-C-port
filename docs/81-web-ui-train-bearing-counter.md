# Web UI train-bearing counter

The browser dashboard now includes a `Train bearings` counter that reflects `clickTrainBearings.length` in engine responses.

This makes the derived train-level bearing summary visible alongside clicks, trains, per-click delay sets, and per-click bearings during operator smoke tests.
