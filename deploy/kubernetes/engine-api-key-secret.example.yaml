# Example only. Replace the value and manage this secret with your deployment tooling.
apiVersion: v1
kind: Secret
metadata:
  name: pamguard-engine-api-key
  labels:
    app: pamguard-engine
type: Opaque
stringData:
  api-key: "change-me"
---
# Deployment patch sketch:
#
# spec:
#   template:
#     spec:
#       containers:
#         - name: engine
#           env:
#             - name: PAMGUARD_API_KEY_FILE
#               value: /var/run/secrets/pamguard/api-key
#           volumeMounts:
#             - name: api-key
#               mountPath: /var/run/secrets/pamguard
#               readOnly: true
#       volumes:
#         - name: api-key
#           secret:
#             secretName: pamguard-engine-api-key
